# 小小账单 · 品牌化与设计语言 —— Eval(机器可判验收合约)

- 日期:2026-07-13
- 配套:spec `docs/superpowers/specs/2026-07-13-品牌化-spec.md` · plan `docs/superpowers/plans/2026-07-13-品牌化.md`
- 状态:待执行

## 铁律

1. **判据只增不删不改**。执行 agent 只允许把条目的 `passes` 从 `false` 翻成 `true`(或写失败备注);要修改/删除判据本身,必须人(用户)确认。
2. 每条翻 `true` 前必须真实执行「验证方式」中的命令/步骤,不允许推断。
3. 全部条目 `passes: true` 且 E2E 组各条通过,才算品牌化完成。

## 白名单(全局定义,防误伤)

以下位置允许出现「小遥 / xiaoyao / dtsola」字样,**不计入**任何"旧品牌残留"判据的命中:

- W1 致谢语境:`README.md`、`README_EN.md` 的致谢/血统声明段、`LICENSE`、`.claude/CLAUDE.md` 1.1 的 fork 说明
- W2 关于页(`frontend/src/views/About.vue`)的「致谢」区块
- W3 本三件套文档与 `docs/06-品牌化研发准备计划.md`、`docs/07-视觉审计报告.md`、`docs/archive/` 归档目录
- W4 git 历史、commit message、`backend/data/` 运行时数据(不检查)
- W5 上游仓库链接 `github.com/dtsola/xiaoyaoprivatebill` 与上游项目专名(任何位置,致谢用途)

---

## BR 组:品牌文案(前端 UI 可见字符串)

| id | 断言 | 验证方式 | passes |
|---|---|---|---|
| BR-01 | 浏览器标签 title 为「小小账单」 | `grep -c '<title>小小账单</title>' frontend/index.html` 输出 `1` | true |
| BR-02 | 侧栏应用名与 logo alt 均为「小小账单」,无「小遥」 | `grep -n '小小账单' frontend/src/components/AppLayout.vue \| wc -l` ≥ 2 且 `grep -c '小遥' frontend/src/components/AppLayout.vue` 输出 `0` | true |
| BR-03 | 登录页主标题为「小小账单」 | `grep -c '<h1>小小账单</h1>' frontend/src/views/Login.vue` 输出 `1` | true |
| BR-04 | AI 欢迎语为「你好，我是小小」 | `grep -c '你好，我是小小' frontend/src/views/AI.vue` 输出 `1` | true |
| BR-05 | AI 页 8 处品牌串全部替换(欢迎语/logo alt/md 引用行/角色名/两处下载文件名/分享图标题/分享图页脚),无「小遥」残留 | `grep -c '小遥' frontend/src/views/AI.vue` 输出 `0` | true |
| BR-06 | 年度报告水印为「{{ year }} 年度账单 · 小小账单」 | `grep -c '年度账单 · 小小账单' frontend/src/views/Annual.vue` 输出 `1` | true |
| BR-07 | 关于页改造完成:`About.vue` 存在,含「关于小小账单」标题、完整致谢链(Hessel2333 与 dtsola 的 GitHub 链接各 ≥1 处)、MIT 双版权说明;dtsola 联系方式/扫码区已移除 | `grep -c 'Hessel2333' frontend/src/views/About.vue` ≥ 1 且 `grep -c 'github.com/dtsola/xiaoyaoprivatebill' frontend/src/views/About.vue` ≥ 1 且 `grep -c '个人二维码\|扫码联系\|微信' frontend/src/views/About.vue` 输出 `0` 且文件 `frontend/src/views/AboutAuthor.vue` 不存在 | true |
| BR-08 | 全源码旧品牌收口:除白名单外,前后端源码 0 处「小遥」 | `grep -rn "小遥" frontend/src frontend/index.html backend --include='*.py' --include='*.js' --include='*.vue' --include='*.html' \| grep -v 'views/About.vue'` 输出为空 | true |

## VI 组:视觉资产与设计统一

| id | 断言 | 验证方式 | passes |
|---|---|---|---|
| VI-01 | 三个 logo/favicon 文件已替换为新品牌视觉(非旧文件) | `shasum -a 256 frontend/public/images/logo_128.png frontend/public/favicon.png frontend/public/favicon.svg` 输出中不含旧哈希前缀 `2018aa327a36c3ab`(png×2 旧值)与 `b69acd5e115f49f3`(svg 旧值) | true |
| VI-02 | 原作者个人物料已从前端静态资源移除 | `ls frontend/public/images/ \| grep -c 'author-avatar\|个人二维码\|开发者交流群\|用户交流群'` 输出 `0` | true |
| VI-03 | 视觉审计报告存在且覆盖全部 21 个 view 页面,逐页给出「符合基准 / P0 / P1」结论 | `docs/07-视觉审计报告.md` 存在;`grep -c '^### ' docs/07-视觉审计报告.md` ≥ 21 | true |
| VI-04 | 审计报告中全部 P0 不一致项已关闭 | 报告内 P0 条目复选框全部勾选:`grep -c '\- \[ \] P0' docs/07-视觉审计报告.md` 输出 `0`(且 `grep -c '\- \[x\] P0'` ≥ 1,若审计结论为「无 P0 项」则本条由用户确认后翻 true 并备注) | true |

## LS 组:localStorage 迁移

| id | 断言 | 验证方式 | passes |
|---|---|---|---|
| LS-01 | 源码写入/读取的业务键全部为 `xiaoxiao_*` 前缀;`xiaoyao_*` 仅允许出现在迁移工具 `frontend/src/utils/storage.js` 内 | `grep -rn "xiaoyao_" frontend/src \| grep -v 'utils/storage.js'` 输出为空 | true |
| LS-02 | 迁移函数 `migrateLegacyStorage()` 有单测:预置 3 个旧键 → 执行 → 3 个新键取到原值且旧键已删除;新键已有值时不覆盖 | `cd frontend && npx vitest run src/__tests__/utils/storage.spec.js` 退出码 0,且该文件断言数 ≥ 4 | true |
| LS-03 | 端到端迁移生效:浏览器 localStorage 预置 `xiaoyao_nav_groups={"analysis":false}` 后刷新应用,导航折叠状态保留且键已更名 | 手动/浏览器自动化:DevTools 预置旧键 → 刷新 → Application 面板确认 `xiaoxiao_nav_groups` 存在含原值、`xiaoyao_nav_groups` 已删除、侧栏分组折叠状态与预置一致 | true |

## DC 组:文档与物料

| id | 断言 | 验证方式 | passes |
|---|---|---|---|
| DC-01 | docs 顶层 10 个 md(00/01/03/04/05/代码架构/开发进度/技术选型/接口/部署)正文「小遥」0 命中(白名单文档除外) | `grep -c "小遥" docs/00-mrd.md docs/01-prd.md docs/03-技术方案文档.md docs/04-开发任务清单.md docs/05-开发排期.md docs/代码架构文档.md docs/开发进度.md docs/技术选型文档.md docs/接口文档.md docs/部署文档.md` 每行输出 `:0` | false |
| DC-02 | 上游运营物料已归档:`docs/运营文档/` 整体移至 `docs/archive/上游运营物料/`,活跃目录不再存在 | `test ! -d docs/运营文档 && test -d docs/archive/上游运营物料 && echo OK` 输出 `OK` | false |
| DC-03 | 新 logo 源文件已入库 `docs/产品文档/logos/`,旧 logo 移至 `docs/archive/` | `shasum -a 256 docs/产品文档/logos/logo_128.png` 不含旧哈希前缀 `2018aa327a36c3ab` | false |

## DP 组:部署与仓库

| id | 断言 | 验证方式 | passes |
|---|---|---|---|
| DP-01 | docker-compose 品牌化:容器名 `xiaoxiao-backend`/`xiaoxiao-frontend`、网络 `xiaoxiao-network`、`APP_NAME=小小账单` | `grep -c 'xiaoxiao-backend\|xiaoxiao-frontend' docker-compose.yml` 输出 `2` 且 `grep -c 'xiaoyao' docker-compose.yml` 输出 `0` | false |
| DP-02 | NAS 生产实例迁移完成且健康(严格「旧 down → 新 up」,volume `backend-data` 数据无损) | `curl -s http://192.168.10.3:8888/ \| grep -c '小小账单'` ≥ 1;登录后既有账单数据可见(用户确认) | false |
| DP-03 | GitHub 仓库改名 `xiaoxiaobill`,本地 origin 已更新 | `gh repo view JokerFuFu/xiaoxiaobill --json name -q .name` 输出 `xiaoxiaobill` 且 `git remote get-url origin` 含 `xiaoxiaobill` | false |
| DP-04 | gitea 仓库改名,本地 gitea remote 已更新且可推送 | `git remote get-url gitea` 含 `xiaoxiaobill` 且 `git ls-remote --heads gitea` 退出码 0 | false |
| DP-05 | README 中英两版内自有仓库链接全部指向新 repo 名(上游 dtsola 链接按 W5 保留) | `grep -c 'JokerFuFu/xiaoyaoprivatebill' README.md README_EN.md` 每行输出 `:0` | false |

## E2E 组:端到端组合判据(至少一条必过,此处三条全要)

| id | 断言 | 验证方式 | passes |
|---|---|---|---|
| E2E-01 | 生产构建成功,且构建产物中「小遥」仅存在于含致谢文本的 chunk(即关于页) | `cd frontend && npm run build` 退出码 0;`grep -l "小遥" dist/assets/*.js \| xargs -r grep -L "Hessel2333" \| wc -l` 输出 `0` | true |
| E2E-02 | 全流程人工走查零旧品牌露出:登录页 → 首页 → 上传 → 年度/月度/分类/时间/渠道 → AI 对话(看欢迎语)→ AI 导出 md+png(看文件名/页脚)→ 年度报告水印 → 关于页 → 浏览器标签 title | 按 plan Task 11 的 9 屏走查表逐屏勾选,任何一屏发现「小遥/旧 logo」即 false | false |
| E2E-03 | 品牌化不破坏功能:后端与前端测试、lint 全绿 | `cd backend && python -m pytest -q` 退出码 0;`cd frontend && npm run test && npm run lint` 退出码 0 | true |

---

## 完成定义

`grep -c 'passes: false\|| false |' 本文件` 归零(全部翻 true),且用户对 E2E-02、DP-02、VI-04 三条人工项签字确认。
