# 小小账单 · 品牌化与设计语言 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把产品呈现层(UI 文案/关于页/logo/localStorage/docs/部署/仓库)从「小遥」全量切换为「小小账单」,设计语言按既有 Apple 风 token 基准收口,全程零功能变更、零用户偏好丢失。

**Architecture:** 纯呈现层改造,按「文案 → 人格 → 关于页 → 存储迁移 → 收口验证 → 视觉资产 → 设计审计 → 文档 → 部署 → 仓库 → 走查」串行推进;三个用户暂停点(挑 logo、审计过目、生产迁移);每个 Task 以 eval 条目为 exit criteria。

**Tech Stack:** Vue 3 + Vite(前端)、Vitest(单测)、Flask(后端,仅 pytest 回归)、Docker Compose(NAS 部署)、gh CLI(仓库改名)。

## Global Constraints

- 命名规范(spec 第 0 段为单一事实来源):中文名「小小账单」、AI 人格「小小」、localStorage 前缀 `xiaoxiao_`、容器/网络前缀 `xiaoxiao-`、仓库名 `xiaoxiaobill`
- **红线:致谢语境的上游专名禁止替换**——「小遥账单助手」「dtsola/xiaoyaoprivatebill」「Hessel2333/alipay_record_analysis」在 README 致谢段、LICENSE、About.vue 致谢区、`.claude/CLAUDE.md` fork 说明中必须保留原文(eval 白名单 W1–W5)
- **eval 铁律**:判据只增不删不改,执行者只把 `passes` 翻 true;eval 文件 = `docs/superpowers/evals/2026-07-13-品牌化-eval.md`
- 不改任何功能行为;后端除已完成的 mailbox 标识外无品牌残留,不需要动后端业务代码
- 工作分支:直接在 `feat/multi-user-and-ai` 上推进;每 Task 独立 commit(Conventional Commits,中文);每完成一个 Task 推送 `gitea` 与 `origin` 双远程
- commit 末尾统一带:`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`

---

### Task 1: 全局 UI 文案替换(标签页/侧栏/登录页/年度水印)

**Files:**
- Modify: `frontend/index.html:6`
- Modify: `frontend/src/components/AppLayout.vue:27-28,203`
- Modify: `frontend/src/views/Login.vue:6`
- Modify: `frontend/src/views/Annual.vue:20`

**Interfaces:**
- Produces: AppLayout 导航项 path 改为 `/about`(Task 3 的路由必须与之一致)

- [ ] **Step 1: index.html 标题**

old → new(精确串):
```
<title>小遥账单助手</title>  →  <title>小小账单</title>
```

- [ ] **Step 2: AppLayout.vue 三处**

```
<img src="/images/logo_128.png" alt="小遥账单助手" class="logo-icon" />
→ <img src="/images/logo_128.png" alt="小小账单" class="logo-icon" />

<span>小遥账单</span>  →  <span>小小账单</span>

{ path: '/about-author', label: '关于作者', icon: 'fas fa-user-circle icon-author' },
→ { path: '/about', label: '关于', icon: 'fas fa-user-circle icon-author' },
```

- [ ] **Step 3: Login.vue / Annual.vue**

```
<h1>小遥账单助手</h1>  →  <h1>小小账单</h1>

<div class="report-brand">{{ year }} 年度账单 · 小遥账单助手</div>
→ <div class="report-brand">{{ year }} 年度账单 · 小小账单</div>
```

- [ ] **Step 4: 验证(eval BR-01/02/03/06)**

```bash
grep -c '<title>小小账单</title>' frontend/index.html          # 期望 1
grep -c '小遥' frontend/src/components/AppLayout.vue           # 期望 0
grep -c '<h1>小小账单</h1>' frontend/src/views/Login.vue        # 期望 1
grep -c '年度账单 · 小小账单' frontend/src/views/Annual.vue     # 期望 1
```

- [ ] **Step 5: Commit**

```bash
git add frontend/index.html frontend/src/components/AppLayout.vue frontend/src/views/Login.vue frontend/src/views/Annual.vue
git commit -m "feat(rebrand): 全局 UI 文案切换为小小账单(标签页/侧栏/登录页/年度水印)"
```

---

### Task 2: AI 人格「小小」(AI.vue 8 处)

**Files:**
- Modify: `frontend/src/views/AI.vue:7,71,482,484,499,518,542,572`

- [ ] **Step 1: 逐处替换(精确串)**

```
alt="小遥"                                    → alt="小小"
<h3>你好，我是小遥</h3>                        → <h3>你好，我是小小</h3>
`> 来自 小遥账单 AI 助手 · ${...}`             → `> 来自 小小账单 AI 助手 · ${...}`
'## 🤖 小遥'                                  → '## 🤖 小小'
a.download = `小遥对话-${title.slice(0, 16)}.md`   → `小小对话-${title.slice(0, 16)}.md`
小遥账单 · ${title}                            → 小小账单 · ${title}
—— 由 小遥账单助手 生成 ——                     → —— 由 小小账单 生成 ——
a.download = `小遥对话-${imgTitle.value.slice(0, 12)}.png` → `小小对话-${imgTitle.value.slice(0, 12)}.png`
```

- [ ] **Step 2: 验证(eval BR-04/05)**

```bash
grep -c '你好，我是小小' frontend/src/views/AI.vue   # 期望 1
grep -c '小遥' frontend/src/views/AI.vue             # 期望 0
```

- [ ] **Step 3: Commit**

```bash
git add frontend/src/views/AI.vue
git commit -m "feat(rebrand): AI 助手人格更名为「小小」(欢迎语/导出/分享图 8 处)"
```

---

### Task 3: 关于页改造(AboutAuthor → About)

**Files:**
- Create: `frontend/src/views/About.vue`
- Delete: `frontend/src/views/AboutAuthor.vue`
- Modify: `frontend/src/router/index.js:22`
- Delete: `frontend/public/images/author-avatar.jpg`、`frontend/public/images/个人二维码.png`、`frontend/public/images/开发者交流群.png`、`frontend/public/images/用户交流群.png`

**Interfaces:**
- Consumes: Task 1 中 AppLayout 导航项已指向 `/about`
- Produces: 路由 `/about`(name: `about`);旧路径 `/about-author` 301 到 `/about`

- [ ] **Step 1: 新建 About.vue(完整内容)**

```vue
<template>
  <div class="about-page">
    <!-- 品牌区 -->
    <section class="card hero">
      <img src="/images/logo_128.png" alt="小小账单" class="hero-logo" />
      <h1>关于小小账单</h1>
      <p class="tagline">隐私优先的个人账单分析工具 · 数据完全本地处理，不上传任何服务器</p>
    </section>

    <!-- 项目介绍 -->
    <section class="card">
      <h2 class="section-title">这是什么</h2>
      <p>
        小小账单把支付宝、微信、银行的账单文件导入<strong>本地</strong>，自动解析、跨源去重，
        生成年度 / 月度 / 分类 / 时间 / 渠道等多维度可视化分析，并内置可选的 AI 账单助手（小小）。
        支持多用户与家庭成员维度、邮箱自动导入、净资产与对账中心。
      </p>
      <p>
        所有数据处理都在你自己的设备上完成；AI 功能完全可选，仅在你主动配置并使用时，
        相关查询结果才会发送到你自己指定的模型端点。
      </p>
    </section>

    <!-- 开源与致谢 -->
    <section class="card">
      <h2 class="section-title">开源与致谢</h2>
      <p>小小账单是一个 MIT 协议的开源项目，站在两位开源作者的肩膀上：</p>
      <ol class="lineage">
        <li>
          <strong>Hessel2333</strong> ——
          <a href="https://github.com/Hessel2333/alipay_record_analysis" target="_blank" rel="noopener noreferrer">alipay_record_analysis</a>，
          最初的灵感与基础代码。
        </li>
        <li>
          <strong>dtsola</strong> ——
          <a href="https://github.com/dtsola/xiaoyaoprivatebill" target="_blank" rel="noopener noreferrer">xiaoyaoprivatebill（小遥账单助手）</a>，
          本项目的直接上游：完成了 Vue 3 + Flask 的现代化重构、模块化蓝图架构与 Docker 部署能力。
        </li>
        <li>
          <strong>小小账单</strong> —— 在上游基础上演进：银行账单接入与跨源去重、转账口径、渠道分析、
          多用户与数据隔离、AI 助手、邮箱自动导入、净资产与对账等。
        </li>
      </ol>
      <p class="license-note">
        许可证：<a href="https://github.com/JokerFuFu/xiaoyaoprivatebill/blob/main/LICENSE" target="_blank" rel="noopener noreferrer">MIT</a>，
        版权声明采用叠加方式——原作品 © dtsola，本项目新增与修改部分 © JokerFuFu。
      </p>
    </section>
  </div>
</template>

<script setup>
// 纯静态展示页,无逻辑
</script>

<style scoped>
.about-page {
  max-width: 760px;
  margin: 0 auto;
  padding: var(--space-6, 24px) var(--space-4, 16px);
  display: flex;
  flex-direction: column;
  gap: var(--space-4, 16px);
}
.card {
  background: var(--card-bg);
  border: 1px solid var(--border-color);
  border-radius: var(--radius-lg, 14px);
  padding: var(--space-6, 24px);
}
.hero { text-align: center; }
.hero-logo { width: 88px; height: 88px; object-fit: contain; margin-bottom: 12px; }
.hero h1 { font-size: 26px; color: var(--text-color); margin: 0 0 8px; }
.tagline { color: var(--secondary-text); margin: 0; }
.section-title { font-size: 18px; color: var(--text-color); margin: 0 0 12px; }
.card p { color: var(--text-color); line-height: 1.75; margin: 0 0 10px; }
.lineage { padding-left: 20px; margin: 0 0 10px; }
.lineage li { color: var(--text-color); line-height: 1.9; }
.lineage a, .license-note a { color: var(--primary-color); text-decoration: none; }
.lineage a:hover, .license-note a:hover { text-decoration: underline; }
.license-note { color: var(--secondary-text); font-size: 13px; }
</style>
```

注:Task 10 完成仓库改名后,本页 LICENSE 链接中的 `xiaoyaoprivatebill` 会由 Task 10 Step 4 统一更新为 `xiaoxiaobill`。

- [ ] **Step 2: 路由更新**

`frontend/src/router/index.js` 第 22 行:

```js
// old
{ path: '/about-author', name: 'about-author', component: () => import('@/views/AboutAuthor.vue') }
// new(两行)
{ path: '/about', name: 'about', component: () => import('@/views/About.vue') },
{ path: '/about-author', redirect: '/about' }
```

- [ ] **Step 3: 删除旧页面与原作者物料**

```bash
git rm frontend/src/views/AboutAuthor.vue \
  frontend/public/images/author-avatar.jpg \
  "frontend/public/images/个人二维码.png" \
  "frontend/public/images/开发者交流群.png" \
  "frontend/public/images/用户交流群.png"
```

- [ ] **Step 4: 手动验证路由**

`cd frontend && npm run dev` → 打开 `http://localhost:3000/about` 正常渲染、`/about-author` 跳转到 `/about`、侧栏「关于」入口可达;致谢链 2 个外链可点。

- [ ] **Step 5: 验证(eval BR-07、VI-02)**

```bash
grep -c 'Hessel2333' frontend/src/views/About.vue                          # ≥1
grep -c 'github.com/dtsola/xiaoyaoprivatebill' frontend/src/views/About.vue # ≥1
grep -c '个人二维码\|扫码联系\|微信' frontend/src/views/About.vue             # 0
test ! -f frontend/src/views/AboutAuthor.vue && echo OK                     # OK
ls frontend/public/images/ | grep -c 'author-avatar\|个人二维码\|开发者交流群\|用户交流群'  # 0
```

- [ ] **Step 6: Commit**

```bash
git add -A frontend/src/views frontend/src/router/index.js frontend/public/images
git commit -m "feat(rebrand): 关于页改造为「关于小小账单」,移除原作者个人推广物料,保留完整致谢链"
```

---

### Task 4: localStorage 前缀迁移(TDD)

**Files:**
- Create: `frontend/src/utils/storage.js`
- Test: `frontend/src/__tests__/utils/storage.spec.js`
- Modify: `frontend/src/main.js`、`frontend/src/stores/session.js:6`、`frontend/src/stores/filter.js:5`、`frontend/src/stores/auth.js:50`、`frontend/src/components/AppLayout.vue:209`

**Interfaces:**
- Produces: `migrateLegacyStorage(storage = localStorage): void`——幂等,旧键搬新键(新键已有值则不覆盖),搬完删旧键,localStorage 不可用时静默

- [ ] **Step 1: 写失败测试**

`frontend/src/__tests__/utils/storage.spec.js`(完整内容):

```js
import { describe, it, expect, beforeEach } from 'vitest'
import { migrateLegacyStorage } from '@/utils/storage'

describe('migrateLegacyStorage', () => {
  beforeEach(() => localStorage.clear())

  it('把三个旧键搬到新键并删除旧键', () => {
    localStorage.setItem('xiaoyao_demo_mode', 'true')
    localStorage.setItem('xiaoyao_transaction_filter', 'expense')
    localStorage.setItem('xiaoyao_nav_groups', '{"analysis":false}')
    migrateLegacyStorage()
    expect(localStorage.getItem('xiaoxiao_demo_mode')).toBe('true')
    expect(localStorage.getItem('xiaoxiao_transaction_filter')).toBe('expense')
    expect(localStorage.getItem('xiaoxiao_nav_groups')).toBe('{"analysis":false}')
    expect(localStorage.getItem('xiaoyao_demo_mode')).toBeNull()
    expect(localStorage.getItem('xiaoyao_transaction_filter')).toBeNull()
    expect(localStorage.getItem('xiaoyao_nav_groups')).toBeNull()
  })

  it('新键已有值时不覆盖,但仍清理旧键', () => {
    localStorage.setItem('xiaoyao_demo_mode', 'true')
    localStorage.setItem('xiaoxiao_demo_mode', 'false')
    migrateLegacyStorage()
    expect(localStorage.getItem('xiaoxiao_demo_mode')).toBe('false')
    expect(localStorage.getItem('xiaoyao_demo_mode')).toBeNull()
  })

  it('无旧键时不产生任何新键', () => {
    migrateLegacyStorage()
    expect(localStorage.getItem('xiaoxiao_demo_mode')).toBeNull()
    expect(localStorage.getItem('xiaoxiao_transaction_filter')).toBeNull()
    expect(localStorage.getItem('xiaoxiao_nav_groups')).toBeNull()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd frontend && npx vitest run src/__tests__/utils/storage.spec.js
```
期望:FAIL(`Failed to resolve import "@/utils/storage"`)。

- [ ] **Step 3: 实现 `frontend/src/utils/storage.js`(完整内容)**

```js
// localStorage 品牌前缀迁移:xiaoyao_* → xiaoxiao_*(2026-07 品牌化)
// 目的:改名不丢用户本地偏好(演示模式/交易筛选/导航折叠状态)
const LEGACY_KEY_MAP = {
  xiaoyao_demo_mode: 'xiaoxiao_demo_mode',
  xiaoyao_transaction_filter: 'xiaoxiao_transaction_filter',
  xiaoyao_nav_groups: 'xiaoxiao_nav_groups',
}

/**
 * 迁移旧品牌前缀的 localStorage 键(幂等)
 * @param {Storage} storage - 默认 window.localStorage,测试可注入
 */
export function migrateLegacyStorage(storage = localStorage) {
  for (const [oldKey, newKey] of Object.entries(LEGACY_KEY_MAP)) {
    try {
      const val = storage.getItem(oldKey)
      if (val !== null) {
        if (storage.getItem(newKey) === null) storage.setItem(newKey, val)
        storage.removeItem(oldKey)
      }
    } catch (e) {
      // 隐私模式等 localStorage 不可用场景:静默跳过
    }
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd frontend && npx vitest run src/__tests__/utils/storage.spec.js
```
期望:3 passed。

- [ ] **Step 5: main.js 挂载迁移(在 createApp 之前)+ 四处键名切新**

`frontend/src/main.js` 在 `import './assets/styles/main.css'` 之后加:

```js
import { migrateLegacyStorage } from './utils/storage'

// 品牌化迁移:必须在任何 store 初始化(读 localStorage)之前执行
migrateLegacyStorage()
```

键名替换(精确串):

```
stores/session.js:6   const DEMO_MODE_KEY = 'xiaoyao_demo_mode'        → 'xiaoxiao_demo_mode'
stores/filter.js:5    const FILTER_KEY = 'xiaoyao_transaction_filter'  → 'xiaoxiao_transaction_filter'
stores/auth.js:50     localStorage.removeItem('xiaoyao_demo_mode')     → localStorage.removeItem('xiaoxiao_demo_mode')
AppLayout.vue:209     const NAV_GROUPS_KEY = 'xiaoyao_nav_groups'      → 'xiaoxiao_nav_groups'
```

- [ ] **Step 6: 全量验证(eval LS-01/02)**

```bash
grep -rn "xiaoyao_" frontend/src | grep -v 'utils/storage.js'   # 期望:空
cd frontend && npm run test                                      # 期望:全绿
```

- [ ] **Step 7: 浏览器端到端(eval LS-03)**

dev server → DevTools Console 执行 `localStorage.setItem('xiaoyao_nav_groups','{"analysis":false}')` → 刷新 → Application 面板确认:`xiaoxiao_nav_groups` = `{"analysis":false}`、旧键消失、侧栏「分析」分组呈折叠态。

- [ ] **Step 8: Commit**

```bash
git add frontend/src/utils/storage.js frontend/src/__tests__/utils/storage.spec.js frontend/src/main.js frontend/src/stores frontend/src/components/AppLayout.vue
git commit -m "feat(rebrand): localStorage 前缀迁移 xiaoyao_→xiaoxiao_,带迁移函数保留用户偏好"
```

---

### Task 5: 源码收口与质量闸

**Files:** 无新改动(纯验证;若发现漏网品牌串,修复后重跑)

- [ ] **Step 1: 全源码残留扫描(eval BR-08)**

```bash
grep -rn "小遥" frontend/src frontend/index.html backend \
  --include='*.py' --include='*.js' --include='*.vue' --include='*.html' \
  | grep -v 'views/About.vue'
```
期望:空输出。若有命中:该处必然是 Task 1–4 的遗漏,按同样规则替换后重跑。

- [ ] **Step 2: 构建产物扫描(eval E2E-01)**

```bash
cd frontend && npm run build
grep -l "小遥" dist/assets/*.js | xargs -r grep -L "Hessel2333" | wc -l   # 期望 0
```

- [ ] **Step 3: 质量闸(eval E2E-03)**

```bash
cd frontend && npm run test && npm run lint      # 全绿
cd ../backend && python -m pytest -q             # 全绿
```

- [ ] **Step 4: 翻 eval + Commit + 推送**

把 eval 文件中 BR-01~08、LS-01/02/03、VI-02、E2E-01、E2E-03 翻 `true`(E2E-03 后续 Task 改动后需复跑,最终以 Task 11 复验为准)。

```bash
git add docs/superpowers/evals/2026-07-13-品牌化-eval.md
git commit -m "chore(rebrand): eval 收口——文案/人格/关于页/存储迁移判据翻绿"
git push gitea feat/multi-user-and-ai && git push origin feat/multi-user-and-ai
```

---

### Task 6: 新 logo 与品牌预览页 ⏸️ 用户暂停点

**Files:**
- Replace: `frontend/public/images/logo_128.png`、`frontend/public/favicon.png`、`frontend/public/favicon.svg`
- Create(过程产物,不入库): scratchpad 下 `brand-preview.html`

- [ ] **Step 1: 生成 3 个 logo 草案**

用 codex-image-gen skill,prompt 要点:「记账/账单 App 图标,极简扁平,圆角方形,主色 #007AFF(Apple 蓝)系,元素可用:票据/账单纸+上升折线,或"小"字几何化;白底可透明;1024×1024」。三草案差异化:①票据+折线 ②"小"字图形化 ③钱币/账本抽象几何。

- [ ] **Step 2: 品牌预览页并请用户挑选**

生成单文件 HTML(所有图 base64 内嵌):三个 logo 草案 × (侧栏 mockup / 登录页 mockup / AI 欢迎屏 mockup) + 色板条(#007AFF/#34C759/#FF3B30/#FF9500 + 灰阶)。**暂停,等用户回复选哪个(或要求重生成)。**

- [ ] **Step 3: 定稿替换三文件**

```bash
# 用 sips 从定稿 1024px 原图出 128px
sips -z 128 128 定稿.png --out frontend/public/images/logo_128.png
cp frontend/public/images/logo_128.png frontend/public/favicon.png
# favicon.svg:据定稿手写简化几何版 SVG(圆角方背景 + 主形状,≤2KB)
```
检查 `frontend/index.html` 中 favicon `<link>` 指向的文件名与实际文件一致。

- [ ] **Step 4: 验证(eval VI-01)**

```bash
shasum -a 256 frontend/public/images/logo_128.png frontend/public/favicon.png frontend/public/favicon.svg
# 输出不含 2018aa327a36c3ab 与 b69acd5e115f49f3
```
浏览器硬刷新确认标签页图标、侧栏、登录页、AI 欢迎屏均为新 logo。

- [ ] **Step 5: Commit**

```bash
git add frontend/public/images/logo_128.png frontend/public/favicon.png frontend/public/favicon.svg
git commit -m "feat(rebrand): 启用小小账单新 logo 与 favicon"
```

---

### Task 7: 视觉审计与 P0 收口 ⏸️ 用户暂停点

**Files:**
- Create: `docs/07-视觉审计报告.md`
- Modify: 审计发现的 P0 页面(以报告为准)

- [ ] **Step 1: 逐页截图审计**

dev server 下逐页截图(21 个 view,登录/演示模式覆盖),对照基准(首页 AI 原生风格 + main.css token)。每页记录:

```markdown
### <页面名>(views/<文件>.vue)
结论:符合基准 | 有 P0 | 仅 P1
- [ ] P0: <具体不一致项:如主按钮硬编码 #1890ff 未用 --primary-color>
- P1(登记不修): <间距/字号刻度偏差等>
```

P0 定义:破坏品牌一致性的显著差异(非 token 主色、明显异类的卡片/按钮风格、与新 logo 冲突的旧视觉元素)。P1:刻度级偏差(留待渐进收敛,呼应 main.css 头部既定约束)。

- [ ] **Step 2: 报告交用户过目**

**暂停,等用户确认 P0 清单范围**(可降级/升级条目)。

- [ ] **Step 3: 修复全部 P0 项**

逐页把硬编码值替换为 `main.css` 既有 token(`--primary-color`/`--card-bg`/`--radius-*`/`--space-*` 等),每改完一页勾掉报告里对应 P0 复选框;禁止顺手做 P1(防样式回归扩面)。

- [ ] **Step 4: 验证(eval VI-03/04)+ Commit**

```bash
grep -c '^### ' docs/07-视觉审计报告.md      # ≥21
grep -c '\- \[ \] P0' docs/07-视觉审计报告.md  # 0
cd frontend && npm run build                  # 成功
git add docs/07-视觉审计报告.md frontend/src
git commit -m "feat(rebrand): 视觉审计收口——全部 P0 不一致项按 token 基准修复"
```

---

### Task 8: docs 批量替换与上游物料归档

**Files:**
- Modify: `docs/00-mrd.md`、`docs/01-prd.md`、`docs/03-技术方案文档.md`、`docs/04-开发任务清单.md`、`docs/05-开发排期.md`、`docs/代码架构文档.md`、`docs/开发进度.md`、`docs/技术选型文档.md`、`docs/接口文档.md`、`docs/部署文档.md`(共 ~15 处「小遥」)
- Move: `docs/运营文档/` → `docs/archive/上游运营物料/`;`docs/产品文档/logos/` 旧 logo、`docs/产品文档/产品截图/作者头像.jpg`、`个人二维码.png`、`宣传海报图.png` → `docs/archive/上游品牌物料/`
- Add: 新 logo 源文件 → `docs/产品文档/logos/`

- [ ] **Step 1: 顶层 md 逐处替换**

先看上下文再改(红线:若该句是"本项目源自小遥账单助手"类致谢语境则**保留并微调为致谢句式**,不得机械替换):

```bash
for f in docs/00-mrd.md docs/01-prd.md docs/03-技术方案文档.md docs/04-开发任务清单.md docs/05-开发排期.md docs/代码架构文档.md docs/开发进度.md docs/技术选型文档.md docs/接口文档.md docs/部署文档.md; do grep -n "小遥" "$f"; done
```
逐处 Edit:产品自称语境 →「小小账单」。

- [ ] **Step 2: 归档**

```bash
mkdir -p docs/archive
git mv docs/运营文档 "docs/archive/上游运营物料"
mkdir -p "docs/archive/上游品牌物料"
git mv "docs/产品文档/logos/logo.png" "docs/产品文档/logos/logo_128.png" "docs/产品文档/logos/logo_512.png" "docs/产品文档/logos/A2.png" "docs/archive/上游品牌物料/" 2>/dev/null || true
git mv "docs/产品文档/产品截图/作者头像.jpg" "docs/产品文档/产品截图/个人二维码.png" "docs/产品文档/产品截图/宣传海报图.png" "docs/archive/上游品牌物料/"
cp <Task6 定稿 1024px 原图> "docs/产品文档/logos/logo_1024.png"
cp frontend/public/images/logo_128.png "docs/产品文档/logos/logo_128.png"
```

- [ ] **Step 3: 验证(eval DC-01/02/03)**

```bash
grep -c "小遥" docs/00-mrd.md docs/01-prd.md docs/03-技术方案文档.md docs/04-开发任务清单.md docs/05-开发排期.md docs/代码架构文档.md docs/开发进度.md docs/技术选型文档.md docs/接口文档.md docs/部署文档.md   # 每行 :0
test ! -d docs/运营文档 && test -d "docs/archive/上游运营物料" && echo OK   # OK
shasum -a 256 "docs/产品文档/logos/logo_128.png"   # 非 2018aa32 前缀
```
README 引用完整性:`grep -oE 'docs/[^)"]+' README.md | while read p; do [ -e "$p" ] || echo "断链: $p"; done` 期望无输出。

- [ ] **Step 4: Commit**

```bash
git add -A docs
git commit -m "docs(rebrand): 设计文档品牌批量更新,上游运营/品牌物料归档至 docs/archive"
```

---

### Task 9: docker-compose 品牌化与 NAS 生产迁移 ⏸️ 需用户在场

**Files:**
- Modify: `docker-compose.yml:2,12,15,30,43,51,63`

**风险预案:** NAS 上 compose 项目名由部署目录名决定,**部署目录名不改**则 named volume `backend-data` 沿用,数据无损;迁移前仍强制备份。

- [ ] **Step 1: 本地修改 docker-compose.yml**

```
# 用于一键启动小遥账单助手        → # 用于一键启动小小账单
container_name: xiaoyao-backend   → container_name: xiaoxiao-backend
- APP_NAME=小遥账单助手            → - APP_NAME=小小账单
container_name: xiaoyao-frontend  → container_name: xiaoxiao-frontend
networks 两处 + 定义处 xiaoyao-network → xiaoxiao-network(共 3 处)
```

- [ ] **Step 2: 验证(eval DP-01)+ 本地冒烟 + Commit**

```bash
grep -c 'xiaoxiao-backend\|xiaoxiao-frontend' docker-compose.yml   # 2
grep -c 'xiaoyao' docker-compose.yml                               # 0
docker compose config -q && echo 语法OK
git add docker-compose.yml && git commit -m "chore(rebrand): docker-compose 容器/网络/APP_NAME 品牌化"
git push gitea feat/multi-user-and-ai && git push origin feat/multi-user-and-ai
```

- [ ] **Step 3: NAS 迁移(参照部署记忆 ssh nas / tar+rsync 流程,严格顺序)**

```bash
ssh nas 'cd <部署目录> && docker-compose down'                 # ① 旧容器下线(此时才释放 8888)
ssh nas 'docker run --rm -v <项目>_backend-data:/data -v /volume1/backup:/bak alpine tar czf /bak/xiaoxiao-data-$(date +%F).tgz -C /data .'   # ② 备份 volume
# ③ 按既有 tar+rsync 流程同步新代码到 NAS 部署目录(目录名不变)
ssh nas 'cd <部署目录> && docker-compose up -d --build'        # ④ 新容器上线
```

- [ ] **Step 4: 验证(eval DP-02)**

```bash
curl -s http://192.168.10.3:8888/ | grep -c '小小账单'   # ≥1
docker ps 确认 xiaoxiao-backend/xiaoxiao-frontend 均 healthy;旧 xiaoyao-* 容器已不存在
```
用户登录确认既有账单数据完好 → 翻 DP-02。

---

### Task 10: 仓库改名与链接收口 ⏸️ 需用户在场

**Files:**
- Modify: `README.md`、`README_EN.md`(仓库链接)、`frontend/src/views/About.vue`(LICENSE 链接)

- [ ] **Step 1: GitHub 改名(用户确认后执行)**

```bash
gh repo rename xiaoxiaobill -R JokerFuFu/xiaoyaoprivatebill
git remote set-url origin https://github.com/JokerFuFu/xiaoxiaobill.git
```

- [ ] **Step 2: gitea 改名(用户在 gitea 网页操作:设置→仓库名→xiaoxiaobill)**

```bash
git remote set-url gitea http://192.168.10.3:3000/joker2/xiaoxiaobill.git
git ls-remote --heads gitea >/dev/null && echo gitea-OK
```

- [ ] **Step 3: 链接批量更新(仅自有仓库链接;上游 dtsola 链接是白名单 W5 保留)**

```bash
grep -rn 'JokerFuFu/xiaoyaoprivatebill' README.md README_EN.md frontend/src/views/About.vue
# 逐处替换为 JokerFuFu/xiaoxiaobill
```

- [ ] **Step 4: 验证(eval DP-03/04/05)+ Commit + 双推**

```bash
gh repo view JokerFuFu/xiaoxiaobill --json name -q .name    # xiaoxiaobill
git remote get-url origin && git remote get-url gitea        # 均含 xiaoxiaobill
grep -c 'JokerFuFu/xiaoyaoprivatebill' README.md README_EN.md  # 每行 :0
git add README.md README_EN.md frontend/src/views/About.vue
git commit -m "chore(rebrand): 仓库改名 xiaoxiaobill,自有链接全量更新(上游致谢链接保留)"
git push gitea feat/multi-user-and-ai && git push origin feat/multi-user-and-ai
```

(可选,用户自行决定:本地目录改名 `mv ~/Projects/community/xiaoyaoprivatebill ~/Projects/community/xiaoxiaobill`——会使已开的 IDE/会话工作目录失效,建议空闲时做。)

---

### Task 11: 截图重制、E2E 走查与 eval 终验

**Files:**
- Replace: `docs/产品文档/产品截图/` 中 README 引用的 9 张(02-年度总览/03-月度分析/04-分类分析/05-时间分析/06-消费洞察-01~03/07-交易记录/08-上传账单)
- Modify: `README.md`(移除「品牌更名前界面」提示行)
- Modify: eval 文件(终验翻绿)

- [ ] **Step 1: 重截 9 张截图**(dev server,演示模式,窗口 1280 宽,文件名与旧图完全一致直接覆盖)

- [ ] **Step 2: README 更新**——删除功能预览区的提示行 `> 以下截图为品牌更名前的界面(应用内旧名「小遥账单助手」),新版视觉更新后将替换。`

- [ ] **Step 3: E2E-02 九屏走查**(每屏看:文案/logo/标签页/导出物)

| # | 屏 | 检查点 | ✓ |
|---|---|---|---|
| 1 | 登录页 | h1、logo、标签页 title+favicon | |
| 2 | 首页 | hero 品牌区 | |
| 3 | 上传(设置) | 面板文案 | |
| 4 | 分析页 5 tab | 图表水印/标题 | |
| 5 | AI 对话 | 欢迎语「我是小小」、头像 | |
| 6 | AI 导出 | md 引用行、png 页脚、两个下载文件名 | |
| 7 | 年度报告 | 水印「· 小小账单」 | |
| 8 | 关于页 | 标题、致谢链外链可点 | |
| 9 | 管理/设置 | 无旧品牌 | |

- [ ] **Step 4: 复跑全部机器判据,eval 全表终验**

依次复跑 eval 中 BR/VI/LS/DC/DP/E2E 每条「验证方式」命令,全部通过后翻绿;E2E-02/DP-02/VI-04 三条人工项请用户签字确认。

- [ ] **Step 5: 终 Commit + 双推**

```bash
git add -A
git commit -m "feat(rebrand): 品牌化收官——截图重制,E2E 走查通过,eval 全绿"
git push gitea feat/multi-user-and-ai && git push origin feat/multi-user-and-ai
```

---

## 自查记录(writing-plans Self-Review)

1. **Spec 覆盖**:spec 范围 IN 的 8 项 ↔ Task 1-2(文案)、3(关于页)、6(logo)、4(localStorage)、7(设计收口)、8(docs/归档)、9(部署)、10(仓库);OUT/CUT 项无 task ✓
2. **无占位符**:所有替换给出精确 old→new 串;About.vue/storage.js/测试为完整代码 ✓
3. **一致性**:`/about` 路由(Task 1 导航 ↔ Task 3 路由)、`migrateLegacyStorage` 签名(Task 4 内部)、`xiaoxiaobill` 仓库名(Task 10 ↔ eval DP 组)、新键名三处一致 ✓
4. **暂停点**:Task 6 Step 2(挑 logo)、Task 7 Step 2(审计过目)、Task 9/10(生产与外部操作)——执行 agent 到达时必须停下等用户 ✓
