# 小遥账单助手 · 迭代优化路线图 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans 在**一次连续执行会话**内从 Phase 0 做到 Phase 4、端到端完成整份计划。Steps use checkbox (`- [ ]`) syntax for tracking。Phase 间以 CI 全绿为自动放行关卡，**不停顿等待人工审批**；仅在文末「一次会话执行约定」的「停下条件」命中时才找人。

**Goal:** 为已功能完备的小遥账单助手补上"测试 + CI"工程地基，逐项清掉已知问题，优化可维护性与体验，并新增报表导出与更多解析器——全程零功能回归。

**Architecture:** 采用"地基先行"的分期策略。Phase 0 建立 pytest / Vitest / GitHub Actions 安全网；后续所有重构与新功能都在这张网的保护下进行。后端为扁平布局 Flask（`conftest.py` 把 `backend/` 注入 `sys.path`），测试直接 `import services.x` / `from parsers.x import ...`；前端为 Vue 3 + Vite，新增 Vitest。

**Tech Stack:** Python 3.10 / Flask / Pandas / pytest；Vue 3.4 / Vite 5 / Pinia / Vitest / ESLint + Prettier；GitHub Actions。

## Global Constraints

以下为全局约束，**每个任务都隐含遵守**（数值/规则逐字取自 Spec）：

- Python 版本：3.10+；`pytest>=7.4.0,<8.0.0`（已在 `backend/requirements.txt`）。
- 后端框架版本：`Flask>=2.0.1,<2.3.0`、`Werkzeug>=2.0.1,<2.3.0`（不升级）。
- 前端栈：Vue 3.4、Vite 5、Pinia 2.1、ECharts 5.4.3；**不引入 TypeScript**。
- API 向后兼容：既有路由路径不变（拆 `api/analysis.py` 为子蓝图时保持 URL）。
- 单文件规范线：目标 < 500 行。
- **测试 fixture 零真实隐私**：所有样本为构造/脱敏数据，严禁提交真实账单入库。
- 视觉：仅打磨现有 Apple 风设计语言，**不换设计语言**。
- 注释/文档用中文；标识符/路径/命令保留英文。
- 提交规范：Conventional Commits，正文中文，结尾附
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 分支：在 `feat/multi-user-and-ai` 上工作（或按需从其切功能分支）。

## 粒度说明（务必先读）

本文件覆盖 5 个 Phase，写在同一文件内，**一次执行会话内连续做完全部 5 个 Phase**。粒度分两档：

- **Phase 0**：完整 bite-sized TDD 步骤（真实代码 + 命令 + 预期输出），直接照做。
- **Phase 1–4**：任务级卡片（精确 Files / 接口 / 完成定义 DoD / 验证命令 / 做法要点）。这些 Phase 涉及重构大文件、视觉打磨、按真实银行样本加解析器——其确切 diff 必须在执行时读到真实代码/样本后才能写出。**遇到不够细的任务卡，就地对着真实代码把它补齐成 bite-sized 步骤（先测试后实现），随即继续执行——不要单独停下来再规划或等待验收。**

---

# Phase 0 · 工程地基（安全网先行）🔴

**目标**：让 `parsers/` 与 `services/analysis.py` 核心有 pytest 回归覆盖；前端有 Vitest 骨架 + ESLint/Prettier；GitHub Actions 每次改动自动跑测试。**DoD：CI 绿，核心解析/分析有回归覆盖。**

## Task 0.1: 后端测试依赖与 analysis 纯函数测试

**Files:**
- Modify: `backend/requirements.txt`（追加 `pytest-cov`）
- Create: `backend/tests/test_analysis_merchants.py`

**Interfaces:**
- Consumes: `from services.analysis import analyze_merchants`；`analyze_merchants(df: pd.DataFrame) -> dict`，返回 `{'merchant_stats': dict, 'frequent_merchants': list[dict]}`，其中 `frequent_merchants` 每项含 `{'name','amount','count','last_visit'}`；仅统计 `df['收/支'] == '支出'` 的记录。
- Produces: 无（首个分析回归测试，作为后续分析测试模板）。

- [ ] **Step 1: 追加测试覆盖率依赖**

在 `backend/requirements.txt` 末尾（`# 测试` 段）追加一行：

```
pytest-cov>=4.1.0
```

- [ ] **Step 2: 写失败测试**

创建 `backend/tests/test_analysis_merchants.py`：

```python
"""services.analysis.analyze_merchants 的回归测试(纯 DataFrame,无文件 fixture)。"""
import pandas as pd

from services.analysis import analyze_merchants


def _sample_df():
    return pd.DataFrame({
        '交易时间': pd.to_datetime(
            ['2024-01-01 12:00', '2024-01-10 12:00', '2024-01-05 08:00']
        ),
        '收/支': ['支出', '支出', '收入'],
        '交易对方': ['商户A', '商户A', '老板'],
        '金额': [30.0, 20.0, 5000.0],
        '交易分类': ['餐饮', '餐饮', '工资'],
    })


def test_analyze_merchants_excludes_income():
    result = analyze_merchants(_sample_df())
    names = [m['name'] for m in result['frequent_merchants']]
    assert '老板' not in names  # 收入不计入商家消费


def test_analyze_merchants_aggregates_expense():
    result = analyze_merchants(_sample_df())
    a = next(m for m in result['frequent_merchants'] if m['name'] == '商户A')
    assert a['amount'] == 50.0     # 30 + 20
    assert a['count'] == 2
    assert a['last_visit'] == '2024-01-10'  # 最近一次
```

- [ ] **Step 3: 运行确认失败（若依赖未装）或直接通过**

Run: `cd backend && python -m pytest tests/test_analysis_merchants.py -v`
Expected: 若 `analyze_merchants` 行为符合断言则 PASS；若报 `ModuleNotFoundError` 先 `pip install -r requirements.txt`。这是一个刻画既有行为的"表征测试"，PASS 即锁定当前正确行为。

- [ ] **Step 4: 提交**

```bash
git add backend/requirements.txt backend/tests/test_analysis_merchants.py
git commit -m "test(analysis): 为 analyze_merchants 补回归测试 + pytest-cov

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

## Task 0.2: 支付宝解析器回归测试（脱敏样本 + TDD 迭代）

**Files:**
- Create: `backend/tests/fixtures/alipay_sample.csv`（**脱敏构造**样本）
- Create: `backend/tests/test_parser_alipay.py`

**Interfaces:**
- Consumes: `from parsers.alipay import parse_alipay_csv`；`parse_alipay_csv(filepath: str) -> pd.DataFrame`。输出契约（稳定，不随格式细节变）：含列 `交易时间`(datetime64)、`金额`、`收/支`、`交易对方`、`商品说明`、`交易分类`、`来源`(全为 `'支付宝'`)、`是否退款`(bool)；退款行金额取负。
- Produces: `backend/tests/fixtures/` 目录约定——所有解析器 fixture 放这里，均为脱敏样本。

- [ ] **Step 1: 构造脱敏支付宝样本**

创建 `backend/tests/fixtures/alipay_sample.csv`。**做法要点**：以一份真实支付宝导出 CSV 为模板，删到 2–3 行、把姓名/账号/单号全部替换成假值（如 `测试商户A`、`test@example.com`）。保留支付宝原始的抬头段与表头结构（解析器靠扫描含 `交易时间` / `交易状态` 的行定位）。**至少含 1 条正常支出 + 1 条 `退款成功` 记录**，以覆盖退款取负逻辑。

- [ ] **Step 2: 写测试**

创建 `backend/tests/test_parser_alipay.py`：

```python
"""支付宝 CSV 解析器契约测试。fixture 为脱敏构造样本。"""
import os

import pandas as pd

from parsers.alipay import parse_alipay_csv

FIXTURE = os.path.join(os.path.dirname(__file__), 'fixtures', 'alipay_sample.csv')


def test_parse_alipay_source_label():
    df = parse_alipay_csv(FIXTURE)
    assert (df['来源'] == '支付宝').all()


def test_parse_alipay_columns_and_dtype():
    df = parse_alipay_csv(FIXTURE)
    for col in ['交易时间', '金额', '收/支', '交易对方', '商品说明', '交易分类', '是否退款']:
        assert col in df.columns
    assert pd.api.types.is_datetime64_any_dtype(df['交易时间'])


def test_parse_alipay_refund_amount_is_negative():
    df = parse_alipay_csv(FIXTURE)
    refunds = df[df['是否退款']]
    assert len(refunds) >= 1          # 样本里至少一条退款
    assert (refunds['金额'] <= 0).all()  # 退款金额取负
```

- [ ] **Step 3: 运行 → 迭代 fixture 直到通过**

Run: `cd backend && python -m pytest tests/test_parser_alipay.py -v`
Expected: 首次可能因 fixture 格式细节 FAIL；按报错调整样本（skiprows/列名/退款状态字样），直到三条断言 PASS。**这是正常 TDD 迭代——用测试反推出一份合法的脱敏样本。**

- [ ] **Step 4: 提交**

```bash
git add backend/tests/fixtures/alipay_sample.csv backend/tests/test_parser_alipay.py
git commit -m "test(parser): 支付宝解析器契约测试 + 脱敏样本

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

## Task 0.3: 其余解析器测试（复用 0.2 模板）

**Files:**
- Create: `backend/tests/fixtures/{wechat_sample.csv, wechat_sample.xlsx, bank_minsheng.txt|pdf, mybank_sample.xlsx}`（均脱敏）
- Create: `backend/tests/test_parser_wechat.py`、`test_parser_bank.py`、`test_parser_mybank.py`

**Interfaces:**
- Consumes: `from parsers.wechat import parse_wechat_csv, parse_wechat_xlsx`；`from parsers.bank import ...`（执行时读 `backend/parsers/bank.py` 确认导出函数名）；`from parsers.mybank import ...`。

- [ ] **Step 1–4（每个解析器重复 Task 0.2 的四步）**

对 wechat / bank / mybank 各建一份脱敏 fixture + 一个契约测试文件，断言：`来源` 标签正确、关键列存在、`交易时间` 为 datetime、方向（收/支）取值合法。**做法与 0.2 完全一致**：先构造脱敏样本 → 写契约断言 → 运行迭代 → 提交。银行 PDF 若不便造 PDF，可先覆盖 `parsers/bank_csv.py` 的文本→DataFrame 路径（纯文本更易造脱敏样本）。

> 银行解析涉及 `pdfplumber` 读取，PDF fixture 构造较重；执行本任务时先读 `backend/parsers/bank.py` 判断哪种输入最易脱敏，再决定用 `.pdf` 还是走 `bank_csv` 文本路径。

- [ ] **提交**：每个解析器测试单独一次提交（信息同 0.2 风格）。

## Task 0.4: 前端 Vitest 骨架 + 首个真实测试

**Files:**
- Modify: `frontend/package.json`（加 devDeps 与 `test` 脚本）
- Create: `frontend/vitest.config.js`
- Create: `frontend/src/utils/__tests__/format.spec.js`

**Interfaces:**
- Consumes: `frontend/src/utils/format.js` 的金额格式化函数（执行时确认导出名——CLAUDE.md 示例为 `formatMoney(1234.5) === '1,234.50'`）。
- Produces: `npm run test` 可跑；`vitest.config.js` 供后续所有前端测试复用。

- [ ] **Step 1: 加依赖与脚本**

`frontend/package.json` 的 `devDependencies` 追加：

```json
"vitest": "^2.1.0",
"@vue/test-utils": "^2.4.6",
"jsdom": "^25.0.0"
```

`scripts` 追加：

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 2: Vitest 配置**

创建 `frontend/vitest.config.js`：

```javascript
import { fileURLToPath } from 'node:url'
import { defineConfig } from 'vitest/config'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) },
  },
  test: {
    environment: 'jsdom',
    globals: true,
  },
})
```

- [ ] **Step 3: 写首个测试（先确认 format.js 导出名，再写断言）**

创建 `frontend/src/utils/__tests__/format.spec.js`：

```javascript
import { describe, it, expect } from 'vitest'
import { formatMoney } from '@/utils/format'

describe('formatMoney', () => {
  it('千分位 + 两位小数', () => {
    expect(formatMoney(1234.5)).toBe('1,234.50')
  })
})
```

> 执行时先打开 `frontend/src/utils/format.js` 核对导出名与实际输出格式；若签名不同，按真实实现改断言（保持"锁定既有行为"的表征测试性质）。

- [ ] **Step 4: 安装并运行**

Run: `cd frontend && npm install && npm run test`
Expected: 1 passed。

- [ ] **Step 5: 提交**

```bash
git add frontend/package.json frontend/package-lock.json frontend/vitest.config.js frontend/src/utils/__tests__/format.spec.js
git commit -m "test(frontend): 引入 Vitest 骨架 + format 首测

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

## Task 0.5: ESLint + Prettier

**Files:**
- Create: `frontend/eslint.config.js`（ESLint 9 flat config）、`frontend/.prettierrc.json`
- Modify: `frontend/package.json`（devDeps + `lint` 脚本）

- [ ] **Step 1: 加依赖与脚本**

`devDependencies` 追加 `eslint@^9`、`eslint-plugin-vue@^9`、`prettier@^3`、`@vue/eslint-config-prettier@^10`。`scripts` 加：

```json
"lint": "eslint . --ext .js,.vue",
"format": "prettier --write \"src/**/*.{js,vue,css}\""
```

- [ ] **Step 2: 配置文件**（flat config，vue3-recommended + prettier 兼容），`.prettierrc.json` 用 2 空格、无分号、单引号、`printWidth: 100`。

- [ ] **Step 3: 首轮 lint 只报告不阻断**

Run: `cd frontend && npm run lint || true`
Expected: 输出问题清单（含 113 处 `console.log` 之类）。**本任务不逐条修**——修复在 Phase 1，这里只让工具就位、CI 可调用。

- [ ] **Step 4: 提交**（`chore(frontend): 引入 ESLint + Prettier`）。

## Task 0.6: GitHub Actions CI

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: 写 CI**

创建 `.github/workflows/ci.yml`：

```yaml
name: CI

on:
  push:
    branches: [main, feat/**]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: pip install -r requirements.txt
      - run: python -m pytest -v

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run test
      - run: npm run build
```

- [ ] **Step 2: 本地预演**

Run: `cd backend && python -m pytest -v` 与 `cd frontend && npm run lint && npm run test && npm run build`
Expected: 全绿（lint 若仍有 error 会红——若阻塞，先把 `lint` 降级为 `eslint . || true` 并在 Phase 1 修复后恢复）。

- [ ] **Step 3: 提交并推送，确认 Actions 变绿**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: 新增 GitHub Actions(后端 pytest + 前端 lint/test/build)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

**Phase 0 DoD 校验**：`.github/workflows/ci.yml` 存在且推送后变绿；`backend/tests/` 含解析器 + 分析测试；`frontend` 可 `npm run test`。

---

# Phase 1 · 修问题（清缺陷 + 文档）🟠

> 依赖 Phase 0。以下为任务卡片；执行时就地对着真实代码补齐为 bite-sized 步骤后连续执行，不中途停下等待验收。

### Task 1.1: 401 处理改走 vue-router
- **Files:** Modify `frontend/src/api/client.js:203-209`
- **做法:** `import router from '@/router'`，把 `window.location.href = '/login'` 改为 `router.push('/login')`；保留"登录页不跳、避免循环"的判断。注意循环 import（router → 若 import client 会环）——如遇环，改为在函数内动态 `import('@/router')`。
- **DoD:** 未登录访问受保护页跳转登录页且浏览器历史正常；新增一个 Vitest：mock `router.push`，断言 401 时被调用、`/login` 路径下不调用。
- **验证:** `cd frontend && npm run test`

### Task 1.2: 收窄裸 except
- **Files:** Modify `backend/utils/file_utils.py:38` 及探索发现的另一处（执行时 `grep -rn "except:" backend/`）。
- **做法:** `except:` → `except (UnicodeDecodeError, Exception) as e` 中最贴切的具体异常；`file_utils.py:38` 是 chardet 容错，改 `except Exception`（并记录 debug 日志）。
- **DoD:** `grep -rn "except:" backend/` 无裸 except；相关既有测试仍绿。

### Task 1.3: console.log 规范化
- **Files:** 前端各处（Vite 构建配置 `frontend/vite.config.js`）。
- **做法:** 生产构建自动剔除——`vite.config.js` 的 `esbuild` 加 `drop: ['console', 'debugger']`（仅 build 生效，dev 保留）；保留少量关键 `console.error`（可用注释豁免或改成统一 `logger`）。
- **DoD:** `npm run build` 产物无 `console.log`；dev 下仍可打印。

### Task 1.4: SDD 三条 fast-follow
- **Files:** `backend/services/mailbox.py`、相关 `backend/tests/test_mailbox_*.py`。
- **做法:** 逐条落实 `.superpowers/sdd/progress.md` 里记录的：① 导入异常处理强化 ② 部分失败标记（一封邮件多附件时区分成功/失败）③ 调度器硬化（异常不拖垮循环）。每条先补测试再改。
- **DoD:** 三条各有对应测试；`pytest tests/ -k mailbox` 全绿。

### Task 1.5: 文档回填
- **Files:** `docs/开发进度.md`、`docs/04-开发任务清单.md`、`docs/接口文档.md`。
- **做法:** 补齐多用户 / AI / 邮箱三块的接口与进度；删除接口文档中标注"已废弃"的老路由；进度表更新到当前分支状态。
- **DoD:** 接口文档与 `backend/api/` 实际路由一致（抽查 auth/ai/mail/networth/reconcile 蓝图）。

### Task 1.6: Monthly.vue 遗留 TODO
- **Files:** `frontend/src/views/Monthly.vue:385`
- **做法:** 执行时读该 TODO，判断实现或删除；若实现则补 Vitest。
- **DoD:** 该文件无遗留 TODO 注释。

**Phase 1 DoD:** 上述问题逐项关闭；CI 保持绿。

---

# Phase 2 · 结构优化（测试保护下重构）🟠

> 依赖 Phase 0 测试护航。**每个拆分任务：先确认相关行为有测试覆盖（不足则补），再拆，拆后跑全套回归。**

### Task 2.1: 拆 `api/analysis.py`(1571) 为子蓝图
- **做法:** 按域拆为 `api/analysis/`（如 `yearly.py`/`monthly.py`/`category.py`/`transactions.py`/`reconcile.py`…），**保持所有 URL 路径不变**（子蓝图挂同一 `url_prefix`）。先为几个关键端点补 API 级测试（Flask test client）锁定响应，再拆。
- **DoD:** 每个新文件 < 500 行；既有 API 路径与响应不变；API 测试绿。

### Task 2.2: 拆 `Insights.vue`(2610) 与 `Settings.vue`(1793)
- **做法:** 按功能块拆子组件（Insights → 桑基/故事/画像/习惯/拿铁 各成组件；Settings → 上传/邮箱/AI/预算/账号 各成面板组件）。父组件只做编排。
- **DoD:** 每个组件 < 500 行；页面功能与交互零回归（本地预览逐块点检 + Vitest 冒烟）。

### Task 2.3: 通用表格组件 + ECharts 封装
- **做法:** 抽 `components/common/DataTable.vue`（供 Transactions/Category 复用）与 `components/common/EChart.vue`（统一 init/resize/dispose，消除 10+ 处重复）。逐页替换。
- **DoD:** 重复的 `echarts.init` 收敛到封装；被替换页面功能不变。

### Task 2.4: 统一 API 响应格式与错误反馈
- **Files:** `frontend/src/api/client.js`、`frontend/src/stores/data.js:92`
- **做法:** 统一后端返回契约的前端解包，消除 `Array.isArray(result)` 之类分支；错误统一走 `ui.showError`。
- **DoD:** `data.js` 无临时兼容分支；错误提示风格一致。

**Phase 2 DoD:** 三个超长文件均 < 500 行；全套测试绿；功能零回归。

---

# Phase 3 · UX / 视觉打磨 🟠

> 依赖 Phase 2（拆分后组件更小、打磨更干净）。执行时首先调用 `design-taste-frontend` 做 audit-first。**视觉主观、无法自动判优劣：agent 尽力打磨并对关键页面截图归档，整轮结束后交人工过目；不因"待审美"中途停顿。**Vitest 只保组件不报错。

### Task 3.1: 设计系统 audit（design-taste-frontend）
- **做法:** 调用 `design-taste-frontend` 对现有 `assets/styles/main.css` 与主要页面做审计，产出"不协调点清单"（色板/间距刻度/字号层级/圆角/阴影/交互态）。**只审计与打磨，不换设计语言。**
- **DoD:** 一份审计清单 + 收敛后的设计 token（CSS 变量）。

### Task 3.2: 左侧导航整理（IA + 折叠逻辑）
- **Files:** `frontend/src/components/AppLayout.vue`、`frontend/src/router/index.js`
- **做法:** 精简/分组导航项（按"分析 / 资产 / 洞察 / 设置"归类，减少一级项数量）；修正折叠逻辑（折叠态图标+悬浮提示、展开态记忆、当前项高亮）。
- **DoD:** 导航一级项明显精简、分组清晰；折叠/展开行为正确且状态持久。

### Task 3.3: 首页布局重构（AI 助手置顶放大）
- **Files:** `frontend/src/views/Home.vue`、`frontend/src/components/MiniChat.vue`
- **做法:** 把 AI 助手（MiniChat）提为首页顶部主视觉、放大占比；KPI 卡片、Top 分类、大额贴条等下移为次要区块。保留既有数据加载逻辑。
- **DoD:** 首页首屏以 AI 助手为主；其余信息层级合理；移动端不错乱。

### Task 3.4: 应用统一设计 token + 交互一致性
- **做法:** 用 3.1 的 token 收敛全站按钮/卡片/表单/hover/focus/loading 态；统一 Toast/Modal 观感。
- **DoD:** 抽查 5+ 页面视觉与交互一致；人工截图验收通过。

**Phase 3 DoD:** 视觉协调、交互统一、导航精简、首页新布局；人工验收通过；CI 绿。

---

# Phase 4 · 新功能 🟢

> 依赖 Phase 0 测试框架。两个功能相互独立，可分别推进。

### Task 4.1: 报表 / 账单导出（PDF / Excel）
- **Files:** 新增 `backend/services/report_export.py` + `backend/api/` 导出端点；前端在 Analysis/Insights 加"导出"入口。
- **做法:** 复用现有分析结果与 AI export 工具；Excel 用 `openpyxl`（已依赖），PDF 用现有 PDF 能力/`pdfplumber` 无法生成→改用轻量方案（执行时评估 `reportlab` 或前端 `html2canvas`+打印）。先写导出函数的单测（给定分析数据→断言文件字节/sheet 结构），再接端点。
- **DoD:** 年度/月度报告可一键导出、内容正确；导出函数有测试。

### Task 4.2: 更多银行 / 来源解析器
- **Files:** `backend/parsers/`（新增来源）、对应 `backend/tests/test_parser_*.py` + 脱敏 fixture。
- **做法:** 每新增一个来源（如某银行 PDF、信用卡账单、京东/拼多多）：先拿脱敏样本 → 按 Task 0.2 模板写契约测试 → 实现解析器输出统一 schema → 接入 `detect_file_source()` 与导入流程。
- **DoD:** 每个新来源有脱敏 fixture + 契约测试；导入链路端到端可用。

**Phase 4 DoD:** 两功能可用、各带测试；CI 绿。

---

## 附:一次会话执行约定

1. **单会话端到端**：从 Phase 0 连续做到 Phase 4，不在 Phase 之间停下等待人工验收。
2. **自展开**：P1–P4 任务卡不够细时，就地对着真实代码补齐为 bite-sized（先测试后实现）后继续。
3. **CI 全绿 = 自动放行关卡**：每个 Phase 收尾必须本地全绿（后端 `python -m pytest`；前端 `npm run lint && npm run test && npm run build`）才进下一个 Phase；红了就地修复。
4. **仅在以下「停下条件」命中时才找人**：① CI 反复修不绿且卡住；② 需要只有用户才有的真实银行/账单样本；③ Phase 3 视觉需人工拍板。
5. **收尾**：全部做完后给出「各 Phase 完成情况 + 提交列表 + 遗留项」总结。
