# 小小账单 · 视觉审计报告(21 屏)

- 日期:2026-07-13
- 审计方式:dev server 演示模式逐页截图 + 全量 style 段硬编码颜色扫描
- 基准:首页 AI 原生风格(commit `547dbfc`)+ `main.css` 既有 token(`--primary-color:#007AFF` 体系)
- P0 定义:破坏品牌一致性的显著差异(非 token 主色、明显异类的卡片/按钮风格、与新 logo 冲突的旧视觉元素);P1:刻度级偏差,登记不修(呼应 main.css 头部「渐进收敛」约束)
- 审计对象:20 个 view + AppLayout 全局层,共 21 屏

---

## 总览

| 结论 | 数量 |
|---|---|
| 符合基准 | 16 |
| 有 P0(候选,见各节) | 5 |
| 仅 P1 | 若干,逐节登记 |

**跨页共性发现(P0-A,涉及 4 个文件)**:「年份/视图筛选 pill、主操作按钮」的激活态强调色跨功能域不统一——分析域为蓝(基准)、渠道页为橙(`#FF9500`)、对账中心为紫(`#5856D6`)、资产负债/收入分析整页为绿(`#34C759`)。两种处理:
- (a) 判 P0:同类控件统一为 `var(--primary-color)`(品牌一致性优先)——**用户已拍板采用(2026-07-13)**
- (b) 判非缺陷:确认「功能域主题色」为有意设计,降级 P1 登记不修——未采用

**跨页共性发现(P1,不修)**:`.btn-primary:hover` 在 6 个文件硬编码 `#0066E6`、Home 为 `#0a6ee0`,视觉不可辨,留待 token 化收敛(建议未来补 `--primary-hover`)。

---

### AppLayout(components/AppLayout.vue)
结论:有 P0
- [x] P0: 密码提醒横幅(pw-banner)使用砖红色系 `#C0392B` / `#A93226` / `#FFF1F0` / `#FBD5D0`(AppLayout.vue:536-562),非 token 危险色体系 → 改为 `var(--danger-color)`(#FF3B30)+ 对应浅底
- P1(登记不修): 演示横幅暖黄(#FFF8E1/#E65100/#F57F17)为有意的提示色,统一出现,保留

### 登录页(views/Login.vue)
结论:符合基准
- P1(登记不修): 提示卡浅蓝系 hex(#f0f7ff/#cfe4ff 等)未 token 化,视觉与主色协调

### 首页(views/Home.vue)
结论:符合基准(本页即 AI 原生风格锚点)
- P1(登记不修): kpi-tag 红绿(#d04437/#1d8a44)为非 token 语义变体;btn-primary:hover #0a6ee0 与他页 #0066E6 不一致(肉眼不可辨)

### 数据分析壳(views/Analysis.vue)
结论:符合基准(tab pill #007AFF,AI 智能分析条紫色 #AF52DE 全 tab 统一)

### 年度总览(views/Yearly.vue)
结论:符合基准
- P1(登记不修): btn-primary:hover #0066E6

### 月度分析(views/Monthly.vue)
结论:符合基准
- P1(登记不修): 同上

### 分类分析(views/Category.vue)
结论:符合基准(「所有/年度/月度」pill 蓝色激活 = 基准形态)
- P1(登记不修): 同上;灰阶 #666/#999 未 token 化

### 时间分析(views/Time.vue)
结论:符合基准(热力图绿色渐变为数据语义色)
- P1(登记不修): btn-primary:hover #0066E6

### 渠道分析(views/Channels.vue)
结论:有 P0(候选,P0-A)
- [x] P0: 年份筛选 pill 激活态为橙色 `#FF9500`(hover `#C77700`),同类控件在分类分析页为蓝 → 统一 `var(--primary-color)`(若确认域主题色设计则降级)
- P1(登记不修): `#FF950014` 等透明度后缀 hex 未 token 化

### 订阅/定期(views/Recurring.vue)
结论:符合基准(类型徽章绿/红/紫为语义分类色,统一出现)

### 境外消费(views/Overseas.vue)
结论:符合基准(空态排版正常)

### 对账中心(views/Reconcile.vue)
结论:有 P0(候选,P0-A)
- [x] P0: 年份筛选 pill 激活态为紫色 `#5856D6`,同类控件跨页第三种颜色 → 统一 `var(--primary-color)`(若确认域主题色设计则降级)
- P1(登记不修): KPI 卡红绿大数字为语义色,保留

### 资产负债(views/NetWorth.vue)
结论:有 P0(候选,P0-A)
- [x] P0: 整页主操作色为绿 `#34C759`(「资产负债表/收入分析」tab 激活、「记一笔快照」「添加」主按钮),与全站蓝主操作色相异 → 控件激活态/主按钮统一 `var(--primary-color)`,语义性的资产/收入绿色数字保留(若确认域主题色设计则降级)

### 收入分析(views/Income.vue,NetWorth 内嵌 tab)
结论:有 P0(候选,P0-A)
- [x] P0: 年份 pill 激活态为绿 `#34C759`(同 NetWorth 主题)→ 同上处理
- P1(登记不修): 收入 KPI 绿色数字为语义色,保留

### 消费洞察(views/Insights.vue)
结论:符合基准(桑基图多彩为数据色;「年度消费故事」紫渐变卡为特色卡,形态统一)
- P1(登记不修): btn-primary:hover #0066E6

### 年度账单(views/Annual.vue)
结论:符合基准(演示模式为空态;有数据态为分享型年度报告专属视觉,紫/黄点缀独立成体系,与产品页面区隔合理)
- P1(登记不修): 大量报告专属 hex(#5856D6/#FFCC00/#4ADE80 等)不并入产品 token

### 交易记录(views/Transactions.vue,/transfers 复用)
结论:符合基准(金额红绿、状态徽章为语义色)
- P1(登记不修): #E4FBE6/#FFE5E5 徽章浅底未 token 化

### AI 助手(views/AI.vue)
结论:符合基准(蓝渐变与首页 hero 同源;错误气泡 #c0392b 出现频率极低)
- 关联项(非独立 P0): 头像/欢迎屏引用 `logo_128.png`(旧 logo 含 "Xiaoyao Bill Assistant" 图内文字),由 Task 6 替换文件自动解决
- P1(登记不修): 错误气泡砖红 #c0392b 与 AppLayout P0 同源,若 P0-1 修复可顺带(不强制)

### 设置(views/Settings.vue)
结论:符合基准(tab pill 蓝;「支付宝账单」蓝/「微信账单」绿为平台品牌色,合理)
- P1(登记不修): btn-primary:hover #0066E6

### 用户管理(views/Admin.vue)
结论:符合基准(代码级审计:#007AFF/#FF9500/#ff3b30 均在系统色板;演示账号无权限,截图待 Task 11 管理员走查兜底)

### 关于(views/About.vue)
结论:符合基准(本次品牌化新建,全 token 化)

---

## P0 修复清单(用户确认「全修」,已全部完成)

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| P0-1 | AppLayout.vue:536-562 | 密码横幅砖红系非 token | `#C0392B`→`var(--danger-color)`,浅底/hover 对应调整 |
| P0-A1 | Channels.vue | 年份 pill 橙色激活态 | → `var(--primary-color)` 或确认域主题色降级 |
| P0-A2 | Reconcile.vue | 年份 pill 紫色激活态 | 同上 |
| P0-A3 | NetWorth.vue | tab/主按钮绿色 | 控件色 → `var(--primary-color)`,语义绿数字保留;或降级 |
| P0-A4 | Income.vue | 年份 pill 绿色激活态 | 同上 |
